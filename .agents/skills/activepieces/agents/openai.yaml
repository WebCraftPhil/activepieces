interface:
  display_name: "Activepieces"
  short_description: "Repo-specific patterns and workflows for activepieces"
  default_prompt: "Use the activepieces repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true